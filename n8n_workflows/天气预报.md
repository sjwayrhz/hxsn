邮件标题
```
{{ $json.city.name }} 未来 5 天温差预报
```
HTML邮件内容
```
{{
`
<div style="font-family: sans-serif; max-width: 500px; border: 1px solid #eee; border-radius: 12px; overflow: hidden; box-shadow: 0 4px 10px rgba(0,0,0,0.05);">
  <div style="background: linear-gradient(135deg, #1e3c72 0%, #2a5298 100%); color: white; padding: 20px; text-align: center;">
    <h2 style="margin: 0; letter-spacing: 1px;">🌟 昆山深度天气预报</h2>
    <p style="margin: 5px 0 0; opacity: 0.9;">未来 5 天详细气象概览</p>
  </div>
  
  <div style="padding: 15px;">
    ${
      Object.values($json.list.reduce((acc, item) => {
        const date = item.dt_txt.split(' ')[0];
        if (!acc[date]) {
          acc[date] = { 
            date, 
            temps: [], 
            desc: item.weather[0].description,
            wind: item.wind.speed,
            humidity: item.main.humidity,
            pop: item.pop, // 降水概率
            vis: item.visibility // 能见度
          };
        }
        acc[date].temps.push(item.main.temp);
        return acc;
      }, {})).slice(0, 5).map(day => `
        <div style="border-bottom: 1px solid #f0f0f0; padding: 15px 5px;">
          <div style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 8px;">
            <div>
              <span style="font-weight: bold; font-size: 16px; color: #333;">${day.date.substring(5).replace('-', '月')}日</span>
              <span style="margin-left: 10px; color: #666; font-size: 14px;">${day.desc}</span>
            </div>
            <div style="text-align: right;">
              <span style="color: #4A90E2; font-size: 15px;">${Math.min(...day.temps).toFixed(1)}°</span>
              <span style="color: #ccc; margin: 0 4px;">/</span>
              <span style="color: #ff6600; font-size: 17px; font-weight: bold;">${Math.max(...day.temps).toFixed(1)}°C</span>
            </div>
          </div>
          
          <div style="display: flex; justify-content: space-between; font-size: 12px; color: #888; background: #f9f9f9; padding: 8px; border-radius: 6px;">
            <span>💧 降水率: ${(day.pop * 100).toFixed(0)}%</span>
            <span>💨 风速: ${day.wind}m/s</span>
            <span>👁️ 能见度: ${(day.vis / 1000).toFixed(1)}km</span>
            <span>💦 湿度: ${day.humidity}%</span>
          </div>
        </div>
      `).join('')
    }
  </div>

  <div style="background: #fdf6ec; padding: 12px; color: #e6a23c; font-size: 13px; text-align: center;">
    昆山天气预报
  </div>
</div>
`
}}
```